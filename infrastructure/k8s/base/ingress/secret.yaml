# cat <<EOF | kubectl apply -f -
# Placeholder only — cert-manager fills this in once the Issuer completes its
# first HTTP-01 challenge against the Ingress above. No need to populate
# tls.key/tls.crt by hand.
apiVersion: v1
kind: Secret
metadata:
  name: command-center-tls-secret
  namespace: command-center
type: kubernetes.io/tls
stringData:
  tls.key: ""
  tls.crt: ""
# EOF
