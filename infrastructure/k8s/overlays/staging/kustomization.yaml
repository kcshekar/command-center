apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: command-center

resources:
  - ../../base

# Staging inherits 1 replica per service from base — no patches needed.
patches: []
